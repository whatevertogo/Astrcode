{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for AstrCode",
  "windows": ["main", "debug-workbench"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:app:allow-app-hide"
  ]
}
